
% https://erlang.org/doc/man/lists.html#foreach-2

% ists:foreach(Func, []]);
% Chars = lists:filter(fun (Char) -> Char < 16#80 end, Str)
% Headers1 = lists:map(fun({K, V}) -> {atom_to_binary(K), V} end, Headers),
